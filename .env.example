PORT=3000
DATA_DIR=./data
NODE_ENV=production

# Optional: host path for downloaded VOD movies/episodes when using
# docker-compose.yml (defaults to ./data/downloads if unset)
# DOWNLOADS_HOST_PATH=./data/downloads

# Portal connections are configured from the web UI's Setup page after the
# app starts — see the Setup page for portals, profiles, and STBEmu import.
