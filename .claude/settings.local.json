{
  "permissions": {
    "allow": [
      "Bash(git rm *)",
      "Bash(git add *)",
      "Bash(git commit -m 'fix: remove non-root user setup — rootless Docker lacks CAP_SETGID/CAP_CHOWN *)",
      "Bash(git push *)"
    ]
  }
}
